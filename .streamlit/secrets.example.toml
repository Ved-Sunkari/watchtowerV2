# Copy these keys into Streamlit Community Cloud secrets.
# For local development, you may also copy this to .streamlit/secrets.toml
# or create a .env file with the same variable names.

CERBRAS_API_KEY = "replace-with-your-cerebras-api-key"
FIRMS_API_KEY = "replace-with-your-nasa-firms-api-key"
